class Point {
  String? x; // Declare instance variable x, initially null.
  String? y; // Declare y, initially null.
}

void cocos() {
  var point = Point();
  point.x = "Me pican";
  point.y = "los cocos";
  print(point.x);
  print(point.y);
}

void ohio() {
  var point = Point();
  point.x = "Muchos dicen de que";
  point.y = "Brrrrr";
  print(point.x);
  print(point.y);
}
